class Transition(object):

    def __init__(self, source, destination, dataTransferMappings):
        self.source = source
        self.destination = destination
        self.dataTransferMapping = dataTransferMappings
